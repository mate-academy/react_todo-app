import React from 'react';
import { TodoApp } from './components/TodoApp';

const App: React.FC = () => {
  return (
    <section className="todoapp">
      <TodoApp />
    </section>
  );
};

export default App;
