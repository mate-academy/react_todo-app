/// <reference types="react-scripts" />

interface Todo {
  title: string,
  userId: number,
  completed: boolean,
  id: number,
}

interface User {
  id: number,
  name: string,
  username: string,
  email: string,
}
