export enum Filter {
  All = 'all',
  Active = 'active',
  Completed = 'completed',
}

export enum ErrorType {
  Get = 'get',
  Post = 'post',
  Delete = 'delete',
  Patch = 'patch',
  isEmpty = 'isEmpty',
}

export enum StorageKey {
  todos = 'todos',
}
